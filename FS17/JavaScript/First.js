/**
 * Created by vladlenov on 07.07.17.
 */
'use strict'; //выполнение кода в строгом режиме, в начале файла, для самоконтроля

// Однострочные комментарии

/*
Многострочные
комментарии
 */

//Переменные

// var a, b, c; // создаются переменные, по умолчанию записывается undefined;
/*
var a; // равносильно var a = undefined;

// var a = 'a',
//    b = 'b',
//    c = 'c';

var APPLE = 'Apple'; //типа константы
var Apple; // конструктор

// Типы данных
var a = 1; // число, можно в любой системе


//alert(a);
var string1 = 'String';
console.log(string1);

typeof 1; //возвращает тип данных
typeof undefined; //возвращает тип данных
typeof true; //возвращает тип данных
typeof null; //возвращает тип данных

var a = 1;
    b = '2',
    c = "hello",
    d = true;
console.log (a + b);

var text = prompt('Ваше имя');
alert(text);

var isEntered = confirm ('Ты тут?');
alert(isEntered);

name = prompt('Как тебя зовут')
switch (name) {
    case 'Петя':
        alert('Привет Петя');
        break;
    case 'Вася':
        alert('Привет Петя');
        break;
    default:
        alert('Гоунах');
        break;
}

условие ? значение : выполнение //тернарный оператор
 */